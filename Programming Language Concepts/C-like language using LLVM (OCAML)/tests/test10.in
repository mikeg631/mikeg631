
void main()
{
    int terms = 64;
    double maxd = 1;
    int j;
    for(j=1; j < terms; ++j)
    {
    maxd = 2*maxd;
    printf("maxd: ");
    printf(maxd);
    }

    printf("Max double ");
    printf(maxd);
    printf(" and j is ");
    printf(j);
}