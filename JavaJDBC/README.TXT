Using the JDBC, we can connect to a MySQL server such as MariaDB and preform queries using prepared statements.
HW11.java contains a GUI application that can allow the user to
* view different sets of Table/column combinations (equalivent to select statements)
* Insert into tables
* Delete from tables
* Update table values
* Manually enter Custom SQL queries.

